# Vercel deployment uses relative paths, so these are not needed in production
# For local development, if you need to call a backend server:
# REACT_APP_API_URL=http://localhost:5000

# OpenAI integration (optional, for Vercel serverless function)
# REACT_APP_OPENAI_KEY=your_key_here
